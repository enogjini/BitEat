name: CI

on:
  push:
    branches: ['**']
  pull_request:

jobs:
  api-tests:
    name: API tests (Node ${{ matrix.node }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: ['20.x', '22.x']

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node }}

      # tesseract.js/multer (invoice scanning) are the suite's first
      # third-party dependencies — everything else it still runs on Node's
      # built-in test runner against hand-written `pg`/OCR doubles, so no
      # network is needed once installed.
      - name: Install dependencies
        run: npm ci

      - name: Run API tests
        run: npm test

  frontend-build:
    name: Frontend build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '22.x'

      # `npm ci` installs exactly what f/package-lock.json says. A bare
      # `npm install` re-resolves react-scripts' optional `typescript` peer
      # and, depending on the npm version, can pick a major that breaks
      # @typescript-eslint (and with it the whole ESLint step of the build).
      - name: Install frontend dependencies
        run: npm ci --prefix f

      - name: Build
        run: npm run build --prefix f
        env:
          CI: false # CRA treats warnings as errors when CI is set
